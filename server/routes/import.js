const express = require('express')
const router = express.Router()

// Placeholder import routes
router.get('/', (req, res) => {
  res.json({ message: 'Import route placeholder' })
})

module.exports = router